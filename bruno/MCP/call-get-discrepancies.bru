meta {
  name: MCP Call - Get Discrepancies
  type: http
  seq: 8
}

post {
  url: {{containerAppUrl}}/mcp
  body: json
  auth: none
}

body:json {
  {
    "jsonrpc": "2.0",
    "id": 8,
    "method": "tools/call",
    "params": {
      "name": "get_discrepancies",
      "arguments": {
        "case_id": "{{caseId}}"
      }
    }
  }
}

assert {
  res.status: eq 200
  res.body.result.content: isDefined
}
